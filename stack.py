import node

head = node.Node(None)

class Stack:

    def __init__(self):
        self.stack = "Stack"
        head = None

    def isEmpty():
        if head == 0:
            return True
        else:
            return False

    def top():
        return head

    def pop():
        x = head
        head = n.getNext
        n.setContent(n.getNext)
        return x

    def push(n):
        if head != 0:
            head.setContent(n.getContent())
        head = n
        
